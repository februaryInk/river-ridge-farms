- flash.each do |message_type, message|
    - if %W[alert danger info notice success warning].include? message_type
        = content_tag(:div, sanitize(message), :class => "flash flash-#{message_type}")